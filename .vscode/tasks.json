{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "ios:rebuild-pods",
      "type": "shell",
      "command": "flutter clean && rm -rf ios/Pods ios/Podfile.lock ios/.symlinks ios/Flutter/Flutter.podspec ios/Flutter/Generated.xcconfig && flutter pub get && (cd ios && pod repo update && pod install)",
      "group": "build",
      "presentation": {
        "reveal": "always"
      },
      "problemMatcher": []
    }
  ]
}
